{
  "name": "api.tecnored.gt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc && copy .env /build/.env",
    "dev": "npx nodemon",
    "migrate": "rimraf ./build && tsc && npx sequelize-cli db:migrate",
    "migrate:undo": "rimraf ./build && tsc && npx sequelize-cli db:migrate:undo:all",
    "seed": "rimraf ./build && tsc && npx sequelize-cli db:seed:all",
    "unseed": "rimraf ./build && tsc && npx sequelize-cli db:seed:undo:all"
  },
  "keywords": [],
  "author": "Axel Aguilar",
  "license": "ISC",
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.1",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "eslint": "^8.34.0",
    "express": "^4.18.2",
    "mysql2": "^3.1.2"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/express": "^4.17.17",
    "@types/sequelize": "^4.28.14",
    "@typescript-eslint/eslint-plugin": "^5.53.0",
    "@typescript-eslint/parser": "^5.53.0",
    "jsonwebtoken": "^9.0.0",
    "nodemon": "^2.0.20",
    "rimraf": "^4.1.2",
    "sequelize": "^6.6.2",
    "sequelize-cli": "^6.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
